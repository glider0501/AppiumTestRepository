[device]
# Device profile name (matches devices/*.json filename)
profile = android_emulator

[app]
# App capabilities profile (matches configs/app/cap/*.json filename)
app_capabilities = swag_labs_v2_7_1
